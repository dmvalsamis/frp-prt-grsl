# Adjudication Memo IA-1 — Independent Pre-Lock Evaluation & Governance Ruling
**Issued:** 2026-08-28 · **Adjudicator:** independent (no prior involvement; judged the bundle `adjudication_bundle_2026-08-28.zip` only) · **Log verbatim in RESEARCH_LOG.md before acting.**

**Verification basis.** Every SHA-256 recomputable from the bundle was recomputed and matches the §8 register: access script REV 3 (`7d48a32a…2f2f`), environment lockfile (`3c602982…01e5`), M2 freeze table (`27b94734…3cfa`, whose interior rows cover dataset_v6_1, normalization params, manifest, e29_selection.json, e210_ensemble.py, m2_ensemble_val_results.json, and the raw baseline results), both baseline JSONs (`d10a2de6…f7b5`, `27c1f967…76dd`), evaluation-standards.md (`147b1b3a…65f3`), and the REV 3 dry-run results (`75ac0978…8528`) and strata (`48fc8a44…d32a`). Gate and audit scripts are not in the bundle; those rows are log-consistent but not independently retestable here. Every binomial figure in the lock package §3 and the walkthrough appendix was independently recomputed and is exact (thresholds ≥20/30, ≥27/42, ≥32/50, ≥38/62; sizes 0.0494/0.0442/0.0325/0.0490; powers 0.894/0.958/0.971/0.994 at p=0.75 and 0.291/0.345/0.336/0.472 at p=0.60; p(12/12)=0.000244, p(9/12)=0.073, p(10/10)=0.00098, p(9/10)=0.0107). The dry-run reproduces the frozen anchors exactly (+0.3131 / +0.3050 / 9–12 vs B6 / 12–12 vs B1), the B-2 JSON's gbdt_log_T1 aggregate equals +0.30496 under the declared 63/0.1 config, and e29_selection.json shows T1×robust as sole R-1′ pairwise dominator with the R-2′ guard unfired (M-3 +0.0212 vs reference +0.0329, within 0.05).

---

## TASK 1 — Findings

### BLOCKERS (must fix before lock)

**IA-B1 — The hashed access script implements a sensitivity statistic that is not the pre-registered one.**
Prereg §6a fixes the block-A sensitivity set at exactly n = 32 = 30 primary + {es_rua_a_2025_0808, pt_trancoso_sao_pedro_2025_0808}; pt_real_2025_0728 and the 14 DEC-3-dropped gate-passers appear in the descriptive table only, never in any paired statistic. But `test_access_v1.py` computes `SECONDARY_sign_test_M2_vs_B6_sensitivity_all` over **all evaluable listed events**, and the event-list schema (`event_name, block, in_primary, d7_restored`) has no flag distinguishing sensitivity members from DEC-3 drops. Two failure branches, both fatal: (a) if the event list contains all 46 gate-passers — as prereg §6's "ALL gate-passing events are reported in the event table regardless" requires — the sensitivity sign test silently includes pt_real and 14 DEC-3 drops, contradicting the locked text at the single access; (b) if the drops are omitted from the CSV, the pre-declared full descriptive table is impossible. The val dry-run structurally cannot catch this (all 13 val flags are `in_primary=True`, so `sensitivity_all == primary` there).
**Fix (one script batch → REV 4, T-4 pattern):**
1. Event-list schema gains `in_sensitivity` (0/1). Block-A values are fully determined now: in_sensitivity = 1 for the 30 primary + es_rua_a + pt_trancoso; 0 for pt_real and the 14 DEC-3 drops. Block B at season close: in_sensitivity = in_primary (no d7-restoration timing issue exists for B; state this in §6a).
2. Sensitivity statistic becomes `sign_test(elig[elig["in_sensitivity"]])`, result key renamed `SECONDARY_sign_test_M2_vs_B6_sensitivity_set`.
3. Freeze the block-A composition **now**: write `resubmission/test_event_list_blockA_LOCKED.csv` (46 rows, all four flags per DEC-1/DEC-3), hash it into §8, and add a runtime assertion that the supplied access list's block-A subset is row-identical to this frozen file. (Today the event list is hashed only in the access-day record — self-attested at run time. The block-A membership must sit inside the lock's hash perimeter, not the operator's.)
4. Re-hash → REV 4; val dry-run revalidation (expected: anchors identical; sensitivity_set == primary on val); REV 4 + new dry-run hashes into §8, REV 1–3 retained as superseded.
This edit conforms the implementation to the statistics **as already pre-declared**; it changes no declared statistic and is therefore inside the hard constraints — and it is only legitimate now, pre-lock.

**IA-B2 — Block-B candidacy horizon and season close are nowhere mechanically defined.**
DEC-6 is a list of procedures; "season close (~Oct)" is judgment. Under the year rule block B = all 2026 ignitions, but the EFFIS refresh date is currently discretionary — a composition lever (a late-igniting 2026 fire is in or out depending on when the refresh is run). A hostile reviewer will find this in one read.
**Fix (add to prereg §3.1 and ACC-1/§5 of the package, exact text):** "Block-B candidacy cutoff: EFFIS episodes with earliest member FIREDATE ≤ 2026-10-31. The final EFFIS refresh and season-close procedure execute no earlier than 2026-11-21 (≥ 21-day data-latency margin, per the right-censoring precedent of 2026-08-24). W-1 window extension may run to latest member FIREDATE + 9 d past the cutoff under the declared rule. Any 2026 ignition with earliest FIREDATE after 2026-10-31 is out-of-candidacy by this pre-declared rule; the count of such events is disclosed at access." Season close thereby becomes a date, not a decision.

**IA-B3 — The program's central temporal claim is stated imprecisely, and as written is false for block A.**
Prereg §1 says the test blocks "postdate all model-development decisions"; the §2 table says block B "postdates the 2026-05-19 SHA-256 model lock"; the §5 M1 bullet (RAT-9.3) says "block A/B events … postdate its lock (2026-05-19) and its Zenodo deposit (2026-06-10) by construction — block A/B events ignite in 2025/2026". 2025 ignitions **precede** 2026-05-19; 2026 ignitions can precede both 2026-05-19 and M2's 2026-08-26 freeze (dz_n_a_2026_0712 ignites in July). Publicly registering a hashed document whose headline sentence is checkably wrong is worse than the supervisor gap this memo replaces.
**Fix (replace in prereg §1, §2 table row, §5 M1 bullet; do not let the walkthrough phrasing seed the manuscript):** the four defensible guarantees, stated as such —
(i) no test-block FRP content, prediction, or forecast-derived statistic was ever computed before the single access (sequestration, logged, verified E1.6a);
(ii) block membership follows the ignition-year rule declared 2026-08-24, before any 2025/2026 gate results existed;
(iii) no block-A or block-B event was ever onboarded or accessed by the original project — M1's training pool contains only ≤ 2024 events, so M1 never saw any test event;
(iv) the subset of block B igniting after 2026-05-19 (M1 deposit) and after 2026-08-26T09:39:40Z (M2 freeze) additionally could not have existed at those locks; report those per-event counts in the access outputs.
§2 block-B row becomes: "sequestered; membership by ignition year; the post-2026-05-19 / post-2026-08-26 igniting subset additionally postdates the model locks (counts reported at access)."

**IA-B4 — All supervisor-dependent text is now unexecutable and must be replaced before hashing.**
Locations: prereg STATUS line and lock-procedure header paragraph; §4 operational line "the supervisor is notified before execution"; §9.1; package status line, §6b.2, and the §7 signature block; the DEC-4 annex ratification line; binding-skill rule G6 ("never lock unilaterally"). Replacement text is specified in the Task 2 procedure below (Steps 0–1). Locking with dead governance language in the hashed text is not acceptable.

**IA-B5 — The §3.1 provisional bracket must resolve now.**
The draft itself forbids the bracket surviving into the locked file, and its resolution trigger ("at signature") no longer exists. **Fix:** adopt D3's 3,000 ha as final text in §3.1, delete the bracket, note "resolved 2026-08-28 under DEC-4 self-certification (IA-1)".

### AMENDMENTS RECOMMENDED

**A-a — W-1 population arithmetic (prereg §3.2).** 66 affected events total vs 41 extended (13 dev + 28 A) + 18 block-B + 3 over-cap (P-1) = 62. Account for the remaining 4 in one sentence (expected: affected candidates later removed by DEC-3/gate/candidacy — name the categories and counts). [Numbering as received: this and the following items are IA-A1…IA-A8.]

**IA-A1 — Carry the 14+1 decomposition of the "15 drops" everywhere the chain appears.**
The log has it right ("15 drops = 14 gate-passers + 1 no-op on a non-passer"), but prereg §9.5, package Disclosure 3, and the walkthrough state "46 gate-passers … 15 drops → 30/32", inviting the spurious arithmetic charge 46 − 15 = 31 ≠ 32. **Fix (exact wording):** "…→ 15 drops (14 gate-passers + 1 non-gate-passer no-op); 46 − 14 = 32 survivors = 30 primary + 2 d7_restored."

**IA-A2 — Runtime hash assertions for the last two unverified inputs.**
The script hash-verifies all 20 model checkpoints and the M1 norm params, but loads `dataset_v6_1.h5` (B6's training data and the normalization source) and the freeze table itself without verification. **Fix (in the IA-B1 REV 4 batch):** in-script constants `FREEZE_SHA256 = 27b94734…3cfa` and `H5_SHA256 = 0a1a7c8a…5724` (and the manifest hash for dry-run mode), asserted before first use.

**IA-A3 — Stale dry-run hash in package §2.**
Package §2's dry-run row cites the REV 2 results hash (`3fbe1ee9…97eb`); the current row is REV 3 (`75ac0978…8528`; strata byte-identical, `48fc8a44…d32a`). **Fix:** update package §2 and state the strata byte-identity. The walkthrough appendix's "access script CURRENT 8217e2b6…e9ae" is likewise stale (REV 2); the sheet is moot, but flag it so it never seeds the manuscript. Both rows update again to REV 4 after IA-B1.

**IA-A4 — AMEND-7's null branch overstates the secondary's inferential status.**
§6 labels the vs-B1 test "descriptive/supporting, no confirmatory claims", yet AMEND-7 says a null primary means the feasibility claim "rests on the secondary vs-persistence test" — a reviewer will ask what inferential weight a non-confirmatory test can bear. **Fix (wording only; no statistic changes):** "…the feasibility claim is supported by the pre-registered secondary vs-persistence comparison, reported with its exact p-value and explicitly labeled secondary — one pre-registered primary hypothesis, no multiplicity adjustment; the secondary licenses a feasibility statement, not a confirmatory advantage claim."

**IA-A5 — Zero-window gate-passers vanish from the per-event table.**
`access()` drops events whose window build yields nothing (`arrays = {… if len(a[0])}`), leaving only a console line — contradicting "ALL gate-passing events are reported in the event table regardless." **Fix (IA-B1 batch):** emit a table row with n_windows = 0, n_fire = 0, evaluable = False, MAE/SS = NaN.

**IA-A6 — Tighten T-3's excerpting license.**
"The manuscript may excerpt" the strata table permits favorable-stratum optics. **Fix:** the manuscript (main text or supplement) reproduces the **full** strata table; prose excerpts are permitted only alongside it.

**IA-A7 — Power note: state the interval on the win-rate estimate.**
9/12 carries a 95% Clopper–Pearson interval of [0.43, 0.95] (verified exact). **Fix (one sentence in package §3):** "The val estimate itself is weakly constrained (9/12; 95% CI 0.43–0.95), which is why the table brackets p_true at 0.60 and 0.75 rather than trusting the point estimate."

**IA-A8 — Register additions (cheap; closes the perimeter's edges).**
(i) `e28_grid_summary.json` — SHA-256 `ab88006baecbbbf7f2327d0dae077a17afc345200d37b9009a1fafeb41f99534` — the 12-run grid behind the selection is cited in §5a but currently sits outside the register. (ii) Record the SHA-256 of the vendored M1 zip alongside Zenodo's md5 (md5 is a provenance echo, not an integrity guarantee). (iii) Write `resubmission/blockA_input_manifest.csv`: SHA-256 of every `{event}_hourly.csv` and `{event}_era5_hourly.csv` for the 46 block-A gate-passers; hash the manifest into §8. Hashing is content-blind — sequestration is untouched — and it converts "the test inputs did not change between lock and access" from an assertion into a check. Commit to the analogous block-B manifest at season close (Task 2, Step 6). (iv) Add the frozen block-A event list from IA-B1 to §8.

### NOTES (no action required)

**IA-N1.** All recomputable register hashes verify (list in the header). The append-only log contains every memo (A2–A6), the 2026-08-28 adversarial review, and their execution entries verbatim, as claimed.
**IA-N2.** The four disclosures are complete and honestly framed after B-1…B-7: DEC-1's primary-exclusion is the correct response to a rule refined after its effect was observable; W-1's uniformity and val byte-identity hold; DEC-3 v1's invalidation is disclosed with both runs preserved and the v2 hash recorded pre-run; B-2's conservativity argument is valid — the log shows M2 frozen at 09:39:40Z before the B-2 order exists.
**IA-N3.** The power note is arithmetically exact, including the unflattering 0.291/0.336 values, and the walkthrough appendix corrects its own sheet's phrasing in the honest direction. This is the standard other prereg sections should be held to.
**IA-N4.** B6's hyperparameters were selected on the same 12 val events used to select M2's cell — symmetric val-optimism on both sides of the comparison, disclosed via the declared 4-config budget. The 9/12 transfer assumption is the real risk and IA-A7's CI sentence is the honest cover.
**IA-N5.** The Wilcoxon path drops NaNs per column independently (`sub[col].dropna()` twice); harmless today because within-evaluable MAEs are never NaN, but brittle — optionally compute on the paired frame in the REV 4 batch.
**IA-N6.** `resubmission-protocol.md` still names the C-FROZEN/C-HONEST ensembles in the access suite (superseded by AMEND-5/RAT-9) and G6 as written; annotate both supersessions when G6′ is written (Step 0). The skill is not a lock document, but it is the binding read-first.
**IA-N7.** The prereg is CRLF-encoded; hash exactly the bytes published and add to the locked-prereg register row: "hash computed over the file as published (CRLF line endings)" — prevents cross-platform re-verification disputes.
**IA-N8.** M1's val dry-run numbers were observed (adversarial review N-3): concur harmless — M1 immutable since 2026-05-19, nothing selectable downstream.
**IA-N9.** The 2026-08-24 within-day ordering (split rule declared before gate results) is supported by the brief's timestamp and the log's append order; defensible if asked.

---

## TASK 2 — Governance ruling: replacement of the supervisor sign-off

**VERDICT: ADOPT WITH MODIFICATIONS.** Self-certified lock + public registration is accepted as the governance step, with these binding modifications: (1) channel requirements are fixed below (Zenodo canonical-required; git tag + release required-supplementary; OSF optional); (2) the mechanism is extended to the two later governance moments — a required season-close deposit and a required post-access deposit — because a single pre-lock timestamp secures the prereg text but not the block-B composition finalization and not the publish-regardless commitment, which are the two remaining places discretion could hide; (3) binding-skill rule G6 is formally amended to G6′ rather than silently violated; (4) the supervisor's declination is logged verbatim; (5) the manuscript carries one explicit disclosure sentence (below).

**Rationale of record.** A private signature is unverifiable by reviewers; a public timestamp is the standard preregistration mechanism and is strictly stronger on the only axis a reviewer can check — immutability and timing. It is strictly weaker on second-human oversight. That loss is real; it is disclosed, not papered over; it is partially offset by the logged adversarial-review rounds, which the manuscript must describe factually as internally executed, AI-assisted review — never as independent oversight. The temporal-sequestration argument is unaffected (and after IA-B3 it will finally be stated precisely). Hard-constraint compliance: nothing in this ruling retrains or reselects M2 or B6, redraws the FINAL split, touches the test blocks, softens the primary comparison, or alters any pre-declared statistic; the IA-B1/IA-A2/IA-A5 script edits conform the implementation to the statistics as pre-declared, pre-lock, which is the only time such conformance edits are legitimate.

**REVISED LOCK PROCEDURE (replaces the walkthrough's 5-step checklist; execute in order):**

**Step 0 — Log the break.** Append to RESEARCH_LOG, verbatim and dated: (a) the supervisor's declination, exact wording ("you are alone on this one"), date received, and context; (b) this memo IA-1 in full; (c) the amendment **G6′**: "G6′ (2026-08-28, per IA-1): the independent governance step is public timestamped registration. No lock is valid unless a third-party-timestamped public deposit of the locked text precedes all test-data access. Private unilateral locking remains forbidden. All supervisor-sign-off language in project documents is superseded." Annotate `resubmission-protocol.md` accordingly (IA-N6).

**Step 1 — Pre-lock edits.** Execute IA-B2, IA-B3, IA-B4, IA-B5, IA-A1, IA-A3, IA-A4, IA-A6, IA-A7, IA-A8(i–ii) as text edits across the prereg, lock package, and DEC-4 annex. Exact replacement blocks:
- Prereg STATUS → "STATUS: LOCKED (self-certified under G6′) — publicly registered before any test access; DOI, tag, and commit in §8 and the package registration record."
- Prereg lock-procedure paragraph → "Lock procedure: this file is frozen, SHA-256-hashed, and publicly registered (Zenodo deposit with DOI; annotated git tag + release on the public frp-prt-grsl repository) with the public timestamp PRECEDING all test-data access. Self-certified under G6′ (supervisor participation declined, logged 2026-08-28); no independent human sign-off exists and none is claimed."
- Prereg §4 operational line → "outputs are written once to resubmission/TEST_ACCESS_RESULTS/ and hashed + logged in RESEARCH_LOG the same day; a publicly timestamped access declaration (the season-close deposit, Step 6 of IA-1) precedes execution. [Supervisor-notification line superseded 2026-08-28.]"
- Prereg §9.1 → "RESOLVED 2026-08-28: supervisor sign-off replaced by self-certified lock + public registration per IA-1 and G6′; gate package, area floor (final text, IA-B5), D4/D7, and DEC-4 are certified by the executor within the registered lock."
- Package §7 signature block → a **Self-certification and public registration record**: executor name and date; G6′ citation; lines for Zenodo DOI, git tag, commit SHA, deposit UTC (filled at Step 4).
- DEC-4 annex ratification line → "DEC-4 (self-certified 2026-08-28 under G6′, disclosed): the executor certifies D1–D5, D7, P-1/P-2 as adopted and D6 as superseded by ACC-1. Certification is bound into the publicly registered lock; no independent ratification was obtained and none is claimed."

**Step 2 — Script REV 4.** One batch: IA-B1 (schema, sensitivity set, frozen block-A list assertion, renamed key), IA-A2 (H5 + freeze-table runtime asserts), IA-A5 (zero-window rows), optionally IA-N5. Re-hash → REV 4; val dry-run revalidation (expected: anchors byte-consistent with REV 3 except run_utc; sensitivity_set == primary on val); REV 4 hash + new dry-run hashes into §8, REV 1–3 retained superseded.

**Step 3 — Freeze block-A composition and inputs.** Write `test_event_list_blockA_LOCKED.csv` (46 rows, four flags) and `blockA_input_manifest.csv` (per-event input-file SHA-256s, IA-A8-iii); hash both into §8.

**Step 4 — Registration = the lock moment.** Assemble the lock bundle: locked prereg, final lock package, final DEC-4 annex, REV 4 script, environment lockfile, block-A list + input manifest, and this memo. Compute the locked prereg's SHA-256; fill its §8 row (the only open row). Then publish, in this order:
- **REQUIRED (canonical):** a **new Zenodo record** (not a version of the model deposit 10.5281/zenodo.20627737 — separate object, separate identity): "Pre-registration: sequestered-test-block evaluation of sub-daily FRP trajectory forecasting (locked)". The DOI timestamp is the lock time.
- **REQUIRED (supplementary):** commit the bundle to the public frp-prt-grsl repository; annotated tag `prereg-lock-v1`; GitHub release attaching the bundle zip. Disclosed as owner-mutable — which is exactly why Zenodo is canonical and this channel is supplementary.
- **OPTIONAL:** OSF registration mirroring the same files.
Enter DOI + tag + commit SHA + UTC into the package registration record and RESEARCH_LOG. From this moment: zero model work until the single access.

**Step 5 — Manuscript wording (September scaffolding).** Governance narrative: "publicly timestamped pre-registration (Zenodo DOI …, git tag …), deposited before any test-data access" replaces every instance of "independent sign-off". Add verbatim: "No independent human sign-off was obtained for the lock; the supervising authority declined participation (logged 2026-08-28). Lock integrity rests on public timestamping preceding all test-data access, the append-only research log, and internally executed adversarial review; the internal review was AI-assisted and is not represented as independent oversight." Forbidden-phrase rules unchanged.

**Step 6 — Season-close deposit (REQUIRED; access is forbidden before it is public).** Execute DEC-6 under the IA-B2 cutoff (candidacy: earliest FIREDATE ≤ 2026-10-31; refresh ≥ 2026-11-21). Produce: final block-B event list (IA-B1 schema), block-B input manifest, gate re-screen outputs, DEC-3-B audit outputs, W-1 extension records, and the out-of-candidacy count. Publish all as **Zenodo version 2** of the prereg record (+ tag `season-close-v1`), including the **access declaration**: intended access window, REV 4 hash restated, operator D. Valsamis, and the sentence "the next deposit will contain the raw access outputs regardless of outcome."

**Step 7 — The access.** Run REV 4 once per ACC-1 and the B-5 technical-failure protocol (unchanged, except the superseded notification line). Same day: hash all outputs, RESEARCH_LOG entry.

**Step 8 — Post-access deposit (REQUIRED, outcome-blind).** Within 72 hours: **Zenodo version 3** with the raw results JSON, strata JSON, per-event CSV, access record, console log, and their hashes — whatever the numbers say. This converts publish-regardless from a promise into a mechanism. Any deviation at any step — including a B-5 re-run — is deposited as a new version **before** proceeding.

**Channel summary:** Zenodo REQUIRED at Steps 4, 6, 8 (canonical timestamp); git tag + release REQUIRED at Step 4 and RECOMMENDED at 6 and 8; OSF OPTIONAL throughout. Pre-Step-4 review of the bundle by the co-author is RECOMMENDED but not required, and is not independent — do not represent it as such.

---

## Overall readiness verdict

**LOCK AFTER FIXES IA-B1 through IA-B5 (with IA-A1–A8 in the same pass), executed via Steps 0–4 above — do not lock today as-is.** The program's substance withstands hostile reading: every recomputable hash verifies, every cited probability is exact, the four disclosures are honest and complete, the freeze ordering is real in the log, and the dry-run reproduces the frozen anchors precisely. The one dangerous finding is IA-B1 — the hashed script's sensitivity statistic silently diverges from the pre-registered set, a defect the val dry-run structurally could not reveal and that would otherwise have surfaced for the first time at the single access, where nothing may be fixed. IA-B2 and IA-B3 close the two remaining lines a hostile reviewer would take (discretionary block-B timing; a checkably false headline sentence in a publicly registered document). The Task 2 mechanism as modified is, on the axis reviewers can verify, stronger than the signature it replaces; its real cost — no second human — is disclosed rather than disguised. One Code session executes Steps 0–4; the lock is then in force.

**Hard-constraint attestation:** this memo orders no retraining, no selection re-run, no new baseline search, no split redraw, no test-block contact, no change to M2, B6, or any pre-declared statistic, and no softening of the primary comparison. VETO-3 stands.

### [END OF MEMO IA-1]
