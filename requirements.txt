# Python 3.11 (CPU-only environment used for the paper). Pinned to the exact
# versions used to produce the committed results. A CPU build of PyTorch is
# sufficient — no GPU is required to train or evaluate the PRT ensemble.
#
#   pip install -r requirements.txt
#   # (for the exact CPU wheel used in the paper:)
#   # pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cpu

torch==2.6.0
numpy==1.26.3
pandas==2.1.4
scikit-learn==1.2.2
scipy==1.11.4
matplotlib==3.8.0
h5py==3.8.0
